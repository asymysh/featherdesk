# This is the spec-first branch: it must contain ONLY specs + project artifacts.
# Source/implementation lives on feature-libav-vp8s8 (Go reference); the Rust
# rewrite will live in its own crates. No code or build output belongs here.

# Rust build output
/target/
**/target/
Cargo.lock
**/*.rs.bk
*.pdb

# Compiled artifacts / native blobs — NEVER commit binaries
*.exe
*.dll
*.dylib
*.so
*.a
*.lib
*.o
*.bz2

# Scratch / OS / editor
/tmp/
*.db
.DS_Store
